386. 最多有k个不同字符的最长子字符串

描述
给定一个字符串，找到最多有k个不同字符的最长子字符串。

样例
例如，给定 s = "eceba" , k = 3,
T 是 "eceb"，长度为 4.

挑战
O(n), n 是所给字符串的长度

思路
LFU算法，在一个长度为k的窗口中，统计每个元素在当前子串中的最新下标
如果有新的元素要加入窗口中，则弹出最新出现最早的元素：

状态
全部通过，虽然只超过了28%的提交。
代码分为两层实现：
下层为mapwindow类，存储数据结构，使用map来控制窗口的移动。
上层为Solution类，统计找出最长的子串长度
