386. 最多有k个不同字符的最长子字符串

描述
给定一个字符串，找到最多有k个不同字符的最长子字符串。

样例
例如，给定 s = "eceba" , k = 3,
T 是 "eceb"，长度为 4.

挑战
O(n), n 是所给字符串的长度

思路
LFU算法，在一个长度为k的窗口中，统计每个元素在当前连续串中出现的最早下标和最新下标
如果有新的元素要加入窗口中，则弹出最新出现最早的元素：

状态
发现一个bug，向堆里push的时候，没有对相同的元素进行更新。fixing
